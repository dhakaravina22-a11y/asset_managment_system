function openDescriptionPopup(primaryControl) {

    var formContext = primaryControl;

    var input = prompt("Enter description:");

    if (input && input.trim() !== "") {
        formContext.getAttribute("ravi_description").setValue(input);

        // Save the record
        formContext.data.save();
    }
}
